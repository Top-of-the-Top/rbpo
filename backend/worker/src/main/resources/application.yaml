spring:
  application:
    name: vedu-worker

  main:
    web-application-type: none
    keep-alive: true

  data:
    redis:
      host: ${REDIS_HOST}
      port: ${REDIS_PORT}
      password: ${REDIS_PASSWORD}
